import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'angularfire-course',
  webDir: 'dist/angular-mobile-app',
  bundledWebRuntime: false
};

export default config;
